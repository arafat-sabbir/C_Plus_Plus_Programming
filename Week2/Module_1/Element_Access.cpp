#include <bits/stdc++.h>
using namespace std;

int main()
{
  string s;
  cin>>s;
//   s.at Gives the value of the specified Index
  cout<<s.at(0);
//   s.back gives the last index value
  cout<<s.back();
//   s.front gives the first index value
  cout<<s.front();
  return 0;
}